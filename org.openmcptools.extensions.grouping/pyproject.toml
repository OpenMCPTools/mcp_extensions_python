[project]
name = "org-openmcptools-extensions-grouping"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.26.0",
]

[tool.uv.sources]
grouping = { path = "src" }
