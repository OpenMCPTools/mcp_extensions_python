[project]
name = "mcp-extensions-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [ "grouping" ]

[tool.uv.sources]
grouping = { path = "./org.openmcptools.extensions.grouping" }
